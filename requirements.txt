# Deployment (local-side only — pushes the kernel, doesn't run the model)
kaggle==2.2.1
 
# Local proxy + test scripts
requests==2.34.2
flask==3.1.3
python-dotenv==1.2.2
 
# Optional: lets tests/*.py and your own scripts use the official OpenAI client
# against the local proxy instead of raw `requests` calls.
# openai==1.54.0
 